/// <reference types="node" />
import * as child_process from "child_process";
export declare class Simctl implements ISimctl {
    launch(deviceId: string, appIdentifier: string, options: IOptions): Promise<string>;
    boot(deviceId: string): Promise<void>;
    terminate(deviceId: string, appIdentifier: string): Promise<string>;
    install(deviceId: string, applicationPath: string): Promise<void>;
    uninstall(deviceId: string, appIdentifier: string, opts?: any): Promise<void>;
    notifyPost(deviceId: string, notification: string): Promise<void>;
    getAppContainer(deviceId: string, appIdentifier: string): Promise<string>;
    getDevices(): Promise<IDevice[]>;
    getLog(deviceId: string, predicate?: string): child_process.ChildProcess;
    private spawnAsync;
    private verifyXcodeCommandLineToolsAreInstalled;
    private simctlSpawn;
}
